@namespace("com.cloudleaf.cloudos.avro.dto")

protocol VendorCredentialsProtocol {
	import idl "sub_detail.avdl";

	record VendorCredentialsES{
        string id;
        string  tenant_id;
        union {null,string}  vendorId=null;
        union {null,string} accountType=null;
        union {null,string} nickname=null;
        union {null,boolean} deleted=null;
        union {null,string} created_at=null;
	}
}
